import { Component, Input } from "@angular/core";
import * as Immutable from "immutable";

@Component({
	selector: "poe-loadouts",
	templateUrl: "loadouts.component.html",
	styleUrls: ["loadouts.component.scss"]
})
export class Loadouts {
	public constructor() { }
}